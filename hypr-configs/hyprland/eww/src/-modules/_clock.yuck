(defwidget Clock []
  (eventbox
    :cursor "pointer"
    :onclick "${EWW_CMD} open calendar --toggle"
    (box
      :orientation "v"
      :space-evenly false
      :class "clock"
      (label :text thour)
      (label :text tmin)
      (label :text tpm))))